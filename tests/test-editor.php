<?php
/**
 * Resize animated GIF Editor Tests.
 *
 * @since   1.0.0
 * @package Resize_Animated_GIF
 */
class RAGIF_Editor_Test extends WP_UnitTestCase {

	/**
	 * Test if our class exists.
	 *
	 * @since  1.0.0
	 */
	function test_class_exists() {
		$this->assertTrue( class_exists( 'RAGIF_Editor' ) );
	}

	/**
	 * Test that we can access our class through our helper function.
	 *
	 * @since  1.0.0
	 */
	function test_class_access() {
		$this->assertInstanceOf( 'RAGIF_Editor', resize_animated_gif()->editor );
	}

	/**
	 * Replace this with some actual testing code.
	 *
	 * @since  1.0.0
	 */
	function test_sample() {
		$this->assertTrue( true );
	}
}
